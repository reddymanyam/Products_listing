import React from 'react'

const ProductListing = () => {
    
  return (
   <>
      
            {lists.map((items)=>(
                <h1>{items.title}</h1>
            )
                
            )}
   </>
  )
}

export default ProductListing